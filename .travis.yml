sudo: required
services:
    - docker

before_install:
    - docker build -t benjam17704/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true benjam17704/docker-react npm run test -- --coverage